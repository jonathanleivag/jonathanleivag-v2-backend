export enum COLLECTION {
  ABOUTMES = 'aboutmes',
  CONTACTS = 'contacts',
  HEROS = 'heros',
}

export enum ENV {
  PORT = 'PORT',
  MONGODB_URI = 'MONGODB_URI',
  DATABASE = 'DATABASE',
  URI = 'URI',
  NODE_ENV = 'NODE_ENV',
  KEY_RESEND = 'KEY_RESEND',
  CONTENT_RESEND = 'CONTENT_RESEND',
  NAME = 'NAME',
  FROM = 'FROM',
  TO = 'TO',
  SUBJECT = 'SUBJECT'
}
